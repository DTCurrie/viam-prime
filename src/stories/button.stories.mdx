import { Canvas, Meta, Story } from "@storybook/addon-docs";

<Meta
  title="Elements/Button"
  parameters={{
    actions: {
      handles: ["click"],
    },
  }}
  argTypes={{
    label: {
      description: "Text displayed to users",
      table: { defaultValue: { summary: "" } },
    },
    variant: {
      description: "The communicated action type of the button",
      control: "select",
      options: [
        "primary",
        "outline-primary",
        "danger",
        "outline-danger",
        "success",
        "outline-success",
      ],
      table: { defaultValue: { summary: "primary" } },
    },
    type: {
      description: "The type attribute for the button",
      control: "select",
      options: ["button", "submit", "reset"],
      table: { defaultValue: { summary: "button" } },
    },
  }}
/>

# Button

For user triggered actions

<Canvas>
  <Story
    name="Primary"
    args={{
      label: "RUN",
    }}
  >
    {({ label, variant }) => `
      <v-button
        label='${label}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Primary (outline)"
    args={{
      label: "RUN",
      variant: "outline-primary",
    }}
  >
    {({ label, variant }) => `
      <v-button
        label='${label}'
        variant="${variant}"
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Danger"
    args={{
      label: "STOP",
      variant: "danger",
    }}
  >
    {({ label, variant }) => `
      <v-button
        variant='${variant}'
        label='${label}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Danger (outline)"
    args={{
      label: "SLOW DOWN",
      variant: "outline-danger",
    }}
  >
    {({ label, variant }) => `
      <v-button
        variant='${variant}'
        label='${label}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Success"
    args={{
      label: "SUCCESS",
      variant: "success",
    }}
  >
    {({ label, variant }) => `
      <v-button
        variant="${variant}"
        label='${label}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Success (outline)"
    args={{
      label: "SUCCESS",
      variant: "outline-success",
    }}
  >
    {({ label, variant }) => `
      <v-button
        variant="${variant}"
        label='${label}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disabled"
    args={{
      disabled: "true",
      label: "RUN",
    }}
  >
    {({ label, disabled }) => `
      <v-button
        label='${label}'
        disabled='${disabled}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disabled (outline)"
    args={{
      disabled: "true",
      variant: "outline-primary",
      label: "RUN",
    }}
  >
    {({ label, disabled, variant }) => `
      <v-button
        label='${label}'
        variant='${variant}'
        disabled='${disabled}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disabled (danger)"
    args={{
      disabled: "true",
      variant: "danger",
      label: "RUN",
    }}
  >
    {({ label, disabled, variant }) => `
      <v-button
        label='${label}'
        variant='${variant}'
        disabled='${disabled}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disabled (outline danger)"
    args={{
      disabled: "true",
      variant: "outline-danger",
      label: "RUN",
    }}
  >
    {({ label, disabled, variant }) => `
      <v-button
        label='${label}'
        variant='${variant}'
        disabled='${disabled}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disabled (success)"
    args={{
      disabled: "true",
      variant: "success",
      label: "RUN",
    }}
  >
    {({ label, disabled, variant }) => `
      <v-button
        label='${label}'
        variant='${variant}'
        disabled='${disabled}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disabled (outline success)"
    args={{
      disabled: "true",
      variant: "outline-primary",
      label: "RUN",
    }}
  >
    {({ label, disabled, variant }) => `
      <v-button
        label='${label}'
        variant='${variant}'
        disabled='${disabled}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="With title"
    args={{
      label: "RUN",
      title: "Run this command",
    }}
  >
    {({ label, title }) => `
      <v-button
        label='${label}'
        title='${title}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="With tooltip"
    args={{
      label: "Hug",
      icon: "naught",
      disabled: "true",
      tooltip: "Hugging is disabled",
    }}
  >
    {({ label, icon, tooltip, disabled }) => `
      <v-button
        label='${label}'
        icon='${icon}'
        disabled='${disabled}'
        tooltip='${tooltip}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="With icon"
    args={{
      label: "Refresh",
      icon: "refresh",
    }}
  >
    {({ label, icon }) => `
      <v-button
        label='${label}'
        icon='${icon}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Icon variant"
    args={{
      icon: "trash",
      variant: "icon",
      label: "Delete component",
    }}
  >
    {({ icon, variant, label }) => `
      <v-button
        icon='${icon}'
        label='${label}'
        variant='${variant}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Icon variant (danger)"
    args={{
      icon: "trash",
      variant: "icon-danger",
      label: "Delete component",
    }}
  >
    {({ icon, variant, label }) => `
      <v-button
        icon='${icon}'
        label='${label}'
        variant='${variant}'
      />
    `}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Icon variant (success)"
    args={{
      icon: "trash",
      variant: "icon-success",
      label: "Delete component",
    }}
  >
    {({ icon, variant, label }) => `
      <v-button
        icon='${icon}'
        label='${label}'
        variant='${variant}'
      />
    `}
  </Story>
</Canvas>
